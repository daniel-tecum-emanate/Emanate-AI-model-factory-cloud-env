# cloud-claim.sh regression suite — results

Generated by `cloud-sessions/tests/run-claims.sh` on 2026-08-20 13:48:27 PDT.

Host bash `3.2.57(1)-release`, git `2.52.0`, python3 `3.14.3`.

Every fetch/push in this run targets a scratch bare repo under a temp directory
(`/var/folders/vg/mwytjzrx77j8443h_nyfq4q40000gp/T/cc-tests.XXXXXX.8MaJGTzNym`), never the real `origin`. Section 1 and the tamper guard at the end prove it.


## 1 — harness self-guards

- **PASS** `harness/origin-is-sandboxed` — the session clone's origin is the scratch bare remote, not GitHub
- **PASS** `harness/help-runs-the-copy` — help exits cleanly against the sandboxed copy
- **PASS** `harness/help-exit-zero` — the command exits 0
- **PASS** `harness/copy-is-identical` — the script under test is a byte-identical copy of bin/cloud-claim.sh

## 2 — check: FREE on a virgin org

- **PASS** `check/free-says-free` — check on a never-claimed org+stream reports FREE
- **PASS** `check/free-exit-zero` — FREE exits 0
- **PASS** `check/stream-defaults-to-per-account` — omitting --stream defaults to per-account and still reads FREE

## 3 — claim: the happy path

- **PASS** `claim/reports-claimed` — a claim on a FREE org+stream succeeds
- **PASS** `claim/exit-zero` — the command exits 0
- **PASS** `claim/remote-held-by` — correct held_by landed on the remote
- **PASS** `claim/remote-org` — correct org_slug landed on the remote
- **PASS** `claim/remote-stream` — correct model_stream landed on the remote
- **PASS** `claim/remote-stage` — correct current_stage landed on the remote
- **PASS** `claim/remote-run-id` — correct run_id landed on the remote
- **PASS** `claim/remote-lane` — correct lane landed on the remote
- **PASS** `claim/remote-heartbeat-ref` — heartbeat_ref points at the holder's own fleet ref
- **PASS** `claim/remote-schema` — schema is 1
- **PASS** `claim/remote-released-at-null` — a fresh claim is not released
- **PASS** `claim/remote-prior-claim-null` — a fresh (FREE-origin) claim has no prior_claim

## 4 — check: MINE for the holder, a distinct claim is untouched by another org

- **PASS** `check/mine-says-mine` — the holder's own check reports MINE
- **PASS** `check/mine-exit-zero` — the command exits 0
- **PASS** `check/other-org-still-free` — a different org+stream is unaffected by acme's claim

## 5 — check/claim: STALE reclaim (the design's §3.8 STALE case)

- **PASS** `stale/no-heartbeat-reads-stale` — a claim whose holder never posted a heartbeat is STALE
- **PASS** `stale/exit-one` — the command exits 1
- **PASS** `stale/prints-the-holder` — the STALE output names who it is reclaiming from
- **PASS** `stale/reclaim-succeeds` — claiming a STALE org+stream succeeds
- **PASS** `stale/reclaim-exit-zero` — the command exits 0
- **PASS** `stale/reclaim-new-holder` — held_by flips to the reclaiming session
- **PASS** `stale/prior-claim-records-old-holder` — prior_claim.held_by names the superseded session
- **PASS** `stale/prior-claim-records-old-stage` — prior_claim.current_stage carries over the superseded stage
- **PASS** `stale/claimed-at-resets` — a reclaim starts a new claim epoch (claimed_at is NOT preserved from the stale claim)
- **PASS** `stale/expired-heartbeat-reads-stale` — an ACTIVE heartbeat older than 30 minutes is STALE, not LIVE
- **PASS** `stale/expired-exit-one` — the command exits 1
- **PASS** `stale/done-status-reads-stale` — a fresh but DONE heartbeat is STALE, not LIVE
- **PASS** `stale/done-status-exit-one` — the command exits 1

## 6 — renew

- **PASS** `renew/reports-renewed` — renew as the holder succeeds
- **PASS** `renew/exit-zero` — the command exits 0
- **PASS** `renew/stage-updated` — current_stage reflects the new stage
- **PASS** `renew/held-by-unchanged` — held_by is unchanged by renew
- **PASS** `renew/claimed-at-preserved` — renew does not reset claimed_at (same claim epoch continues)
- **PASS** `renew/refuses-non-holder` — renew refuses when the caller does not hold the claim
- **PASS** `renew/refuses-non-holder-exit-two` — the command exits 2
- **PASS** `renew/non-holder-attempt-did-not-change-stage` — a refused renew changes nothing on the remote
- **PASS** `renew/refuses-when-nothing-claimed` — renew on an org with no claim at all refuses
- **PASS** `renew/refuses-when-nothing-claimed-exit-two` — the command exits 2

## 7 — release

- **PASS** `release/reports-released` — release as the holder succeeds
- **PASS** `release/exit-zero` — the command exits 0
- **PASS** `release/reason-recorded` — release_reason is recorded
- **PASS** `release/released-at-set` — released_at is set
- **PASS** `release/check-after-release-is-free` — check after release reports FREE again
- **PASS** `release/check-after-release-exit-zero` — the command exits 0
- **PASS** `release/refuses-non-holder` — release refuses for a session that never held the claim
- **PASS** `release/refuses-non-holder-exit-two` — the command exits 2
- **PASS** `release/refuses-already-released` — releasing an already-released claim is refused, not a silent no-op
- **PASS** `release/refuses-already-released-exit-two` — the command exits 2

## 8 — LIVE: refuses cleanly and never overwrites (design §3.8's load-bearing path)

- **PASS** `live/check-says-live` — check against a genuinely live claim reports LIVE
- **PASS** `live/check-exit-three` — the command exits 3
- **PASS** `live/check-names-the-holder` — the LIVE output names who holds it
- **PASS** `live/check-shows-heartbeat-status` — the LIVE output surfaces the dereferenced heartbeat status
- **PASS** `live/claim-refuses` — claiming an org+stream with a live holder is refused
- **PASS** `live/claim-refuses-exit-three` — the command exits 3
- **PASS** `live/claim-refusal-does-not-touch-the-ref` — a refused LIVE claim leaves the existing claim commit byte-for-byte unchanged on the remote
- **PASS** `live/held-by-still-the-original-holder` — held_by after the refused attempt is still the original session
- **PASS** `live/blocked-still-reads-live` — a BLOCKED (not just ACTIVE) fresh heartbeat still means LIVE, not reclaimable
- **PASS** `live/blocked-exit-three` — the command exits 3
- **PASS** `live/blocked-surfaces-the-blocker` — check surfaces the blocked holder's own blocker kind/detail/needs
- **PASS** `live/blocked-surfaces-detail` — the blocker detail is surfaced

## 9 — real concurrent claim: exactly one winner, the ref never corrupts

- **PASS** `race/exactly-one-winner` — exactly one of two truly concurrent claim attempts on a FREE org succeeds (0), the other refuses (3)
- **PASS** `race/remote-converges-to-the-winner` — the remote's held_by is exactly the process that exited 0
- **PASS** `race/exactly-one-ref-created` — the race produced exactly one claim ref for raceco/per-account, not two divergent ones

## 10 — validation and refusal paths

- **PASS** `validate/unknown-stream-refused` — an unrecognised --stream is refused, not silently accepted
- **PASS** `validate/unknown-stream-exit-two` — the command exits 2
- **PASS** `validate/missing-org-refused` — check with no --org is refused
- **PASS** `validate/missing-org-exit-two` — the command exits 2
- **PASS** `validate/claim-missing-stage-refused` — claim with no --stage is refused
- **PASS** `validate/claim-missing-stage-exit-two` — the command exits 2
- **PASS** `validate/renew-missing-stage-refused` — renew with no --stage is refused
- **PASS** `validate/renew-missing-stage-exit-two` — the command exits 2
- **PASS** `validate/release-missing-reason-refused` — release with no --reason is refused
- **PASS** `validate/release-missing-reason-exit-two` — the command exits 2
- **PASS** `validate/release-unknown-reason-refused` — an unrecognised --reason is refused
- **PASS** `validate/release-unknown-reason-exit-two` — the command exits 2
- **PASS** `validate/stage-on-check-refused` — --stage does not belong to check and is refused
- **PASS** `validate/stage-on-check-exit-two` — the command exits 2
- **PASS** `validate/lane-on-renew-refused` — --lane does not belong to renew and is refused
- **PASS** `validate/lane-on-renew-exit-two` — the command exits 2
- **PASS** `validate/no-session-id-refused` — no $CLAUDE_CODE_SESSION_ID and no --session is refused
- **PASS** `validate/no-session-id-exit-two` — the command exits 2
- **PASS** `validate/bad-session-id-refused` — a session id unsafe as a filename/ref is refused
- **PASS** `validate/bad-session-id-exit-two` — the command exits 2
- **PASS** `validate/org-path-traversal-refused` — an --org containing '..' is refused
- **PASS** `validate/org-path-traversal-exit-two` — the command exits 2
- **PASS** `validate/org-leading-dot-refused` — an --org starting with a non-alphanumeric character (e.g. a bare ../etc) is refused too, by the charset guard
- **PASS** `validate/org-leading-dot-exit-two` — the command exits 2
- **PASS** `validate/unknown-verb-refused` — an unknown verb is an error, not a fallthrough
- **PASS** `validate/unknown-verb-exit-two` — the command exits 2
- **PASS** `validate/help-lists-verbs` — help documents all four verbs
- **PASS** `validate/help-exit-zero` — the command exits 0

## 11 — the dispatch block extracts mechanically and matches the design

- **PASS** `dispatch/extracts-nonempty` — the mechanical extraction command yields non-empty text
- **PASS** `dispatch/mentions-check-verb` — the extracted block tells the agent to run check first
- **PASS** `dispatch/mentions-all-four-exit-codes` — the extracted block documents the STALE case
- **PASS** `dispatch/covers-live-stop` — the extracted block tells the agent to STOP on LIVE
- **PASS** `dispatch/orders-renew-before-beat` — the extracted block pairs renew with the heartbeat beat
- **PASS** `dispatch/orders-release-before-terminal-heartbeat` — the extracted block releases before the terminal heartbeat call
- **PASS** `dispatch/exactly-one-fence-pair-open` — exactly one '# ```text' fence marker exists (a second would corrupt the sed range extraction)
- **PASS** `dispatch/exactly-one-fence-pair-close` — exactly one closing '# ```' fence marker exists

## 12 — tamper guard: nothing real was touched

- **PASS** `tamper/real-cloud-claim-sh-unchanged` — bin/cloud-claim.sh itself was not modified by running this suite
- **PASS** `tamper/real-origin-unchanged` — the real repo's own origin remote is unchanged
- **PASS** `tamper/no-suite-commit-in-real-repo` — no commit authored by this suite's own synthetic git identity (tests@example.invalid) ever reached the real repo — deliberately NOT a raw HEAD-equality check, since this repo sees legitimate concurrent commits from other sessions during a run
- **PASS** `tamper/real-claims-state-unchanged` — the real repo's cloud-sessions/state/claims/ tree is byte-identical before and after
- **PASS** `tamper/real-local-refs-unchanged` — no refs/cloud-claims/* or refs/heads/cloud-claims/* ref exists in the real repo

## Summary

- PASS: 111
- FAIL: 0
- SKIP: 0
